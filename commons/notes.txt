- x bibliotega flag -- pode usar pra porta, host, variavel de ambiente
- x entender como funciona go modules

service: 
- completar os atributos do block

client:
- x Verificar possiveis mudanças na chamada pro grpc

BigQuery: 
- acesso gcp
- conexão e consulta

possiveis mudanças:
- x transformar o message do proto para uma unica string que contém o json da struct do block


final se sobrar tempo:
- bibliotega grpc -
- biblioteca grpc/credentials -> passa a credencial na hora de criar a conexão: grpc.WithTransportCredentials - newTLS
redes:
- estudar TLS, camada de segurança
- porque registra o server no reflect apos o pb.RegisterBlocksServer

/*
    protoc --go_out=commons commons/proto/*.proto --go-grpc_out=commons
*/